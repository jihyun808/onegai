# 미결정 사항 / 검토 필요

결정이 필요하거나 나중에 다시 봐야 할 항목을 적어둔다.

## 남은 작업

2026-08-08 기준.

### 완료

| 항목 | 비고 |
| --- | --- |
| `GET /api/search` | 정규화·그룹핑·캐싱·페이지네이션, 테스트 66개 |
| 금영 공식 연동 | 폴백 체인 + 헬스체크 + stale 캐시 |
| TJ 공식 연동 | 한글 발음 검색 확보, manana 폴백 |
| 가사 검색 (`type=lyrics`) | 금영 가사 인덱스 + TJ 번호 역조회 |
| 번역 / 미리듣기 / 가사 | 키 없어도 graceful |
| **가사보기** | 금영에서 한글 발음까지, 없으면 검색 링크 — 30번 (권리는 37번) |
| **썸네일 / 미리듣기** | iTunes, 누른 카드만 조회 — 아래 31번 |
| **자체 카탈로그 DB** | 8.6만 곡, 검색 5초 → 0.03초 (+ 공식 보강) |
| 테스트 | 서버 168개 · 클라이언트 45개 |
| `GET /api/health` | Redis 상태 포함 |
| 홈(검색) 화면 | 손그림 UI, 디바운스 300ms, 무한스크롤 |
| 마이페이지 | 즐겨찾기 목록 + 정렬 (로컬 저장) |
| 하단 네비게이션 | 3탭, 곡선 바 + 중앙 홈 버튼 |
| 로고 | 잘난체 워드마크 + 음표 |
| Docker Compose | server / redis / mysql (+ client 프로필) |
| 운영용 WSGI | gunicorn(gthread) — 아래 34번 |
| 인증 | 가입·로그인·탈퇴, bcrypt·시도 제한·httpOnly 쿠키 — 27번 |
| 요청 제한 | IP 단위, Redis 기반 — 28번 |
| 서버 즐겨찾기 | 가입 시에만 로컬 목록 이전 — 7번 |
| 설정 화면 | 한국곡 토글·정렬·데이터·정보 — 29번 |
| 금영 DB 공백 메우기 | Ado 금영 5 → 69건 (16번, 해결됨 / 곡 수는 1번 노이즈 포함) |

### 미착수

| 항목 | 막고 있는 것 |
| --- | --- |
| **개인정보처리방침·이용약관** | 가입을 받는데 문서가 없다 (`SettingsPage.tsx` TODO) |
| **크롤링 자동 실행** | `crawl-daily`는 있으나 아무것도 안 돌림 — 아래 15번 |
| **장애 알림** | 헬스체크는 있으나 알려주지 않음 — 아래 10번 |
| 인기곡 화면 | manana `/karaoke/popular` 미연동 — 아래 9번 |
| 비밀번호 찾기 | 이메일을 안 받아서 되돌릴 방법이 없다 |
| 카테고리 분류 | 보컬로이드/애니/게임 태그 — 미착수 |

`search_history` 테이블은 아직 연결하지 않았다. 검색어를 남길 실익이
있는지부터 정해야 한다 (개인정보 관점에서도).

---

## 1. 가수 검색이 부분 문자열로 걸린다 (B안 적용, 2026-08-19)

**상태:** 아래 B안(토큰 단위 일치)으로 구현했다. 규칙은 36번 8항에 정리했다.
단 한글 발음 검색에는 여전히 안 걸린다 — 같은 항의 한계 참고.

**manana만의 문제가 아니다** — 금영 공식(kysing.kr)도 똑같이 부분일치다.
`Ado` 검색 99건 중 실제 Ado 곡은 22건이고, 나머지 77건은 `ADOY`,
`Owen Ov**ado**z`, `Plac**ido** Domingo` 같은 글자 겹침이다 (2026-08-19 실측).

**이 노이즈가 문서의 통계까지 오염시켰다.** 5번·16번 표의 곡 수를
검색 건수로 세는 바람에 "금영 공식 Ado 99곡"처럼 4배 넘게 부풀었다.
두 표는 정정했다. **앞으로 카탈로그 크기를 셀 때는 반드시 가수명을 대조한다.**

### 증상

가수 검색에 무관한 결과가 섞인다.

```bash
curl "http://localhost:5001/api/search?q=Ado&type=singer&brand=tj"
```

```json
{
  "brand": "tj",
  "no": "74089",
  "title": "HEY CHILD",
  "singer": "X Ambassadors",   // ← 'Ado' 검색인데 'X Ambassadors' 가 나옴
  "release": "2026-07-27"
}
```

`X Ambassadors` 안에 `ado`(Amb**ado**rs)가 포함되어 매칭된 것으로 추정된다.
짧은 검색어일수록 노이즈가 심해질 가능성이 높다.

### 왜 서버에서 아직 안 걸렀나

업스트림 동작이고, 어디까지가 노이즈인지는 기획 판단이 필요하다.
예를 들어 `아이유` 검색 시 `High4,아이유` 같은 피처링 곡은 **원하는** 결과인데,
단순히 "가수명이 정확히 일치하는 것만" 필터링하면 이것까지 사라진다.

### 선택지

| 방안 | 내용 | 트레이드오프 |
| --- | --- | --- |
| A. 필터 없음 (이전) | 업스트림 결과 그대로 | 노이즈 노출 |
| **B. 토큰 단위 일치 (채택)** | `split_singers()`로 쪼갠 뒤 정규화 비교 | 피처링 곡 유지, `X Ambassadors` 제거. 표기 흔들림에 취약 |
| C. 정렬만 조정 | 정확 일치를 상단에 노출, 나머지는 뒤로 | 결과 손실 없음. UI에서 처리 필요 |
| D. 클라이언트 처리 | 서버는 그대로, 앱에서 토글 | 유연하지만 트래픽 낭비 |

### 참고

B안에 필요한 재료는 이미 있다 — `app/utils/normalize.py`의
`split_singers()`, `normalize_text()`, 그리고 각 결과에 붙는 `match_key`.
`app/services/search_service.py`의 `search()`에서 정렬 직전에 필터를 끼우면 된다.

---

## 2. 브랜드 범위 (결정됨)

**결정:** `tj`, `kumyoung` 만 지원한다.

manana API 자체는 `joysound`, `dam`, `uga`(일본 기기)도 지원하지만,
기획 의도가 **한국 노래방에서 일본곡 찾기**이므로 국내 기기만 노출한다.

- `app/services/manana.py`의 `SUPPORTED_BRANDS` 한 줄로 관리된다.
- 나중에 일본 기기까지 확장한다면 `SUPPORTED_BRANDS`에 추가하면 된다.

### 주의: 브랜드를 생략한 manana 호출은 쓰면 안 된다

`/karaoke/singer/{q}.json`(브랜드 미지정)은 전 기기 결과의 합집합이 아니라
**잘린 다른 데이터셋**을 반환한다.

| 검색어 | 무브랜드 응답의 tj | tj 직접 호출 |
| --- | --- | --- |
| `Ado` | 10건 | 70건 |
| `YOASOBI` | 0건 | 32건 |

`YOASOBI`는 무브랜드 응답에 joysound/dam/uga만 176건 들어 있고
tj·kumyoung은 아예 없다. 무브랜드 응답을 걸러 쓰면 결과가 통째로 비어버린다.

그래서 `brand=all`은 지원 브랜드를 **각각 호출해서 합친다**
(`search_service.search()`). 업스트림 호출이 브랜드 수만큼 늘어나지만,
결과는 24시간 캐싱되므로 실사용 비용은 낮다.

---

## 3. 괄호 제거가 버전 구분까지 지운다 (트레이드오프, 확인 필요)

**상태:** 의도된 동작이지만 UI 판단이 필요할 수 있음

정규화 규칙 3번(괄호 안 부가정보 제거)은 부가정보뿐 아니라
**버전 표기까지** 지운다. 그래서 같은 브랜드 안에서도 병합이 일어난다.

```
아이유 검색 → kumyoung
  '미아(Acoustic Ver.)'      ┐
  '미아'                      ┴→ 한 그룹, brands: {kumyoung: ['88397','46438']}
  '그대네요 (여자 선택)'        ┐
  '그대네요'                   ┴→ 한 그룹, 번호 3개
```

번호는 전부 보존되므로 **정보 손실은 없다**. 다만 그룹의 대표 `title`은
하나만 남으므로, 목록에서 `(여자 선택)` / `(Acoustic Ver.)` 구분이 사라진다.

- 노래방에서는 남녀 키 구분이 중요하니, 그룹 안에 번호가 2개 이상이면
  UI에서 펼쳐 보여주는 편이 나을 수 있다.
- 원본 제목이 필요하면 `results`(브랜드별 원본 목록)에 그대로 있다.

---

## 4. 브랜드별 가수 등록명이 다른 경우 (한국 가수 한정)

정규화로는 해결 안 되는 층위다.

| 검색어 | TJ | 금영 |
| --- | --- | --- |
| `IU` | 211건 | - |
| `아이유` | 1건 | 129건 |

TJ는 아이유를 `IU`로, 금영은 `아이유`로 등록한다.
표기 정규화가 아니라 별칭(alias) 사전이 필요한 문제다.

**단, 일본 가수는 양쪽 다 일본어 표기로 통일되어 있어 이 앱의 주 용도에는 영향이 적다.**
(`ヨルシカ` → TJ 31건 / 금영 10건, `米津玄師` → TJ 35건 / 금영 17건.
 `Yorushika`로는 양쪽 다 0건)

---

## 5. 금영 데이터가 2026-04부터 수집 중단 (해결됨 — 공식 사이트 직접 조회)

**최신곡뿐 아니라 검색에도 영향을 준다.** 금영 카탈로그 전체에 2026-04 이후
release가 **0건**이다. 구곡은 남아 있어 검색되므로 문제가 잘 안 보일 뿐이다.

### 일본 가수 기준 확인 (2026-08-05)

| 가수 | TJ 최신 | 금영 최신 |
| --- | --- | --- |
| Ado | 2026-07-27 | 2026-03-01 |
| YOASOBI | 2026-07-30 | 2023-08-01 |
| ヨルシカ | 2026-07-28 | 2023-04-01 |
| 米津玄師 | 2026-03-31 | 2023-02-01 |
| King Gnu | 2026-05-07 | 2022-07-01 |
| Official髭男dism | 2026-07-23 | 2023-09-01 |
| tuki. | 2026-07-30 | 없음 |

### 원인이 아닌 것 (확인 완료)

- **브랜드 코드**: `/karaoke/brand.json` → `["kumyoung","tj","dam","joysound"]`.
  `ky`라는 코드는 존재하지 않는다. 우리는 처음부터 `kumyoung`을 쓴다.
- **API 버전**: v1과 v2가 동일한 결과를 준다.
  `/v2/karaoke/release/202607/kumyoung.json` → `row: 0`
- **엔드포인트 선택**: 인기곡(`/karaoke/popular/kumyoung/daily`)도
  최신 release가 2025-12-01로 멈춰 있다. 수집 파이프라인 전체의 문제로 보인다.
- 참고로 joysound도 2025-05 이후 갱신이 없다.

### 해결: 금영 공식 사이트 직접 조회 (2026-08-05 적용)

금영 공식(kysing.kr) 검색은 스크래핑이 가능하고, manana가 놓친 곡을 갖고 있다.

**아래 수치는 가수명이 정확히 일치하는 것만 센 값이다** (2026-08-19 재실측).
그냥 검색 건수를 세면 안 된다 — 1번의 부분일치 노이즈가 그대로 들어온다.
예전에 이 표에 적혀 있던 `Ado 99곡`이 그 오류였다. 99건 중 실제 Ado 곡은 22곡이고
나머지는 `ADOY`, `Owen Ovadoz`, `Placido Domingo` 같은 글자 겹침이었다.

| 가수 | 금영 공식 | manana 금영 | 공식에만 |
| --- | --- | --- | --- |
| ヨルシカ | 20 | 10 | 10곡 |
| Ado | 22 | 5 | 17곡 |
| 米津玄師 | 30 | 17 | 13곡 |
| YOASOBI | 26 | 17 | 9곡 |

- `GET https://kysing.kr/search/?category=7&keyword={q}&s_page={n}`
- `category`: 2 곡명 / 7 아티스트 / 4 가사 / 1 곡번호 / 5 작곡가 / 6 작사가
- **페이지네이션은 `s_page`** (한 페이지 15건). 이걸 못 찾으면 15건에서 잘린다.
- 출시월(`2025.11`)까지 제공되어 manana 스키마로 그대로 변환된다.
- ~~**국가·장르 필터는 없다.**~~ **2026-08-19 정정** — 검색 페이지에는 없지만
  '노래방 책'(`/karaoke-book/?city=jp`)에 일본곡 색인이 있다. 40번 참고.

TJ는 manana가 거의 따라잡고 있어(가수당 0~3곡 차이) 그대로 둔다.
공식을 하나만 쓰면 깨질 표면적도 절반이다.

### 깨질 것을 전제로 한 방어

스크래핑은 언젠가 깨진다. **다만 앱이 아니라 서버에서 일어나므로
앱 스토어 심사 없이 즉시 고칠 수 있다.** 세 겹으로 막아 둔다.

1. **폴백 체인** — 금영은 `공식 → 실패/0건이면 manana`.
   예외, 파서 크래시, 조용한 0건 모두 폴백한다 (`_fetch_brand()`).
   최악의 경우가 "예전 상태로 복귀"이지 "앱 다운"이 아니다.
2. **파서 헬스체크** — `GET /api/health?deep=1`.
   결과가 있어야 마땅한 검색어(`ヨルシカ`)를 던져 행 수와 필드를 확인한다.
   금영만 깨지면 `partial`, manana까지 죽으면 `degraded`.
   결과는 5분 캐싱해 헬스체크가 외부를 두들기지 않게 한다.
   ⚠️ **물어봐야 답하는 구조다. 알림은 아직 없다** — 아래 10번 참고.
3. **캐시 완충재** — 정상 캐시(24h)와 별개로 비상용 사본(7일)을 남긴다.
   모든 소스가 실패하면 만료된 사본이라도 내보내고 응답에 `stale: true`를 붙인다.

`KYSING_ENABLED=false`로 끄면 manana만 쓴다 (폴백과 같은 동작).

### 원인 제공자에게 문의

manana 운영자(yks118) Discord — https://discord.gg/dZFASsU
manana가 복구되면 공식 조회를 꺼도 된다.

**2026-08-19 — 운영자 회신을 받았으나 데이터는 아직 그대로다.**

회신 요지: 금영 수집이 PHP 버전 업으로 문법 오류가 나서 멈춰 있었고,
수정을 마쳐 갱신이 정상화됐다. JOYSOUND는 정상이며 문제되는 URL을 알려주면
확인해 주겠다.

같은 날 실측한 결과는 5일 전과 동일했다. 위 표의 누락이 하나도 안 줄었고,
manana 금영 데이터의 최신 출시일은 여전히 `2024-05`(아이유 기준)이다.
2025년 등록곡은 전 가수 통틀어 0건이다. 같은 시각 TJ는 `2026-07-30`,
JOYSOUND는 `2026-08-07`까지 정상이라 **금영만 멈춰 있다.**

Ado·米津玄師의 금영 데이터에 `2026-03-01`, `2026-09-01`짜리 항목이 보이지만
곡번호가 5만번대인 구곡이라 신규 등록이 아니라 출시일 필드 노이즈다.

**해석:** 오류 수정으로 앞으로의 수집은 살아났더라도, 고장나 있던
2024년 중반~2026년 8월 구간은 저절로 채워지지 않는다. **그 구간의 재크롤을
요청해야 한다.** 회신용 근거는 `docs/manana-report.md`에 정리해 두었다.

그때까지 `KYSING_ENABLED=true`를 유지한다. 같은 날 금영 공식 스크래핑은
정상 동작을 확인했다 (HTTP 200, 페이지당 15행, `LyricsCont` 포함).

---

## 6. 빈 결과 캐싱 TTL (결정됨, 재검토 여지 있음)

검색 결과가 있으면 24시간(`CACHE_TTL`), 없으면 10분(`EMPTY_CACHE_TTL`) 캐싱한다.

업스트림 일시 장애나 오타 검색으로 얻은 빈 결과가 24시간 동안 굳는 것을 막기 위함이다.
운영하면서 manana 호출량이 부담되면 늘리는 것을 검토한다.

---

## 7. 즐겨찾기 (구현됨, 2026-08-06)

로그인 여부에 따라 저장 위치가 다르다.

| 상태 | 위치 |
| --- | --- |
| 비로그인 | 기기 `localStorage` — 로그인 없이도 쓸 수 있어야 한다 |
| 로그인 | 서버 `favorites` 테이블 — 기기를 바꿔도 남는다 |

```
GET    /api/favorites            곡 단위로 묶어서 반환
POST   /api/favorites  {songs}   한 곡도, 통째로 올릴 때도 같은 형태
DELETE /api/favorites/<brand>/<no>
```

**저장은 번호 단위, 화면은 곡 단위다.** 테이블은 `(user_id, brand, song_no)`라
태진만 담긴 곡도 표현되고, 응답에서는 검색과 같은 정규화·그룹핑을 거쳐
한 카드로 묶인다. 덕분에 클라이언트가 `SongCard`를 그대로 재사용한다.

### 로그인 시 이전

로그인·가입 직후 로컬에 쌓인 것을 서버로 한 번 올리고 로컬을 비운다
(`migrateLocal`). 안 그러면 그동안 담은 것이 사라진 것처럼 보인다.

- **올리기에 성공해야만 로컬을 비운다.** 실패했는데 지우면 데이터가 사라진다.
- 서버에 이미 있는 곡은 UNIQUE 제약이 조용히 무시하므로 여러 번 불려도 안전하다.
- 이전에 실패해도 로그인 자체는 성공시킨다. 로컬에 그대로 남아 다음에 다시 시도된다.

실측: 비로그인으로 2곡 담기 → 가입 → 서버에 2곡, 로컬 비워짐, 새로고침 후에도 유지.

### 담고 지울 때

화면을 먼저 바꾸고 서버에 보낸다(낙관적 갱신). 실패하면 되돌린다.
북마크는 누르자마자 반응해야 하는 조작이라 왕복을 기다리게 하지 않는다.

---

## 8. 카드의 썸네일 / 재생 / 가사보기 (구현됨, 2026-08-06)

manana에는 앨범아트·가사·영상이 없다. 외부 API로 해결했다 (2026-08-05).

| 슬롯 | 해결 | 키 |
| --- | --- | --- |
| 썸네일 | iTunes Search `artwork_url` | 불필요 |
| ▶ 재생 | iTunes Search `preview_url` (30초) | 불필요 |
| 가사보기 | 금영 검색 결과에 딸려 온다 (아래 30번) | 불필요 |
| 한국어 제목 | DeepL | `DEEPL_API_KEY` |

셋 다 카드에 붙었다. 가사는 30번, 썸네일·재생은 31번 참고.

주의할 점:
- 카드마다 호출하면 목록 50개에 요청 50건이 된다. **화면에 보이는 카드만
  지연 호출하거나, 카드를 펼쳤을 때만 부르는 편이 낫다.**
- 미리듣기·가사는 키가 없으면 `available: false`로 오므로 UI를 조건부로 그린다.
  `/api/health`의 `features`로 미리 판단할 수 있다.

---

## 9. 인기곡 — 화면은 가능, "J-pop만" 필터는 불가

**결정 대기.**

`/karaoke/popular/{brand}/{daily|weekly|monthly}` 는 100건을 준다.
스키마가 검색과 **완전히 동일**하다 — `brand, no, title, singer, composer, lyricist, release`.
즉 **장르 필드가 없고, 순위 숫자 필드도 없다** (배열 순서가 곧 순위).

문제는 이 차트가 한국 대중가요 위주라는 것이다 (2026-08-05 확인).

| | 인기곡 100 | 그중 일본어 표기 포함 |
| --- | --- | --- |
| TJ | 100건 | 9건 |
| 금영 | 100건 | 0건 |

일본곡 판별을 제목·가수의 일본어 문자 유무로 하는 것은 휴리스틱이라
로마자 표기 일본곡을 놓친다.

| 방안 | 결과 |
| --- | --- |
| A. 인기곡 100 그대로 | 한국곡 위주 목록 |
| B. 일본어 휴리스틱 필터 | TJ 9곡, 금영 0곡 — 너무 적음 |
| C. 넣지 않음 | 현재 상태 |

## 참고: 최신곡 기능은 제거됨 (2026-08-05)

`/api/latest`와 `manana.latest()`를 삭제했다. 제공하지 않기로 결정.
되살리려면 manana의 `/karaoke/{brand}.json`(브랜드당 20건)을 쓰면 되고,
브랜드를 생략한 `/karaoke.json`은 2번 항목의 이유로 쓰면 안 된다.

---

## 10. 장애 알림이 없다 (배포 시 할 일)

**상태:** 미착수. 배포 전이라 아직 못 붙임.

`/api/health?deep=1`은 **물어봐야 답한다.** 아무도 안 물어보면 파서가 깨진 채로
방치된다. 실제로 알림을 받으려면 아래 둘 중 하나가 필요하다.

### ① 외부 감시 서비스 (배포 직후 바로, 권장)

UptimeRobot / Better Stack 등에 등록한다.

- 대상: `https://<배포주소>/api/health?deep=1`
- 주기: 5분
- 조건: 응답 본문에 `"degraded"` 또는 `"partial"` 포함 시 알림
- 알림: 메일 / 모바일 푸시

**코드 0줄, 무료 플랜으로 충분하다.** 그리고 결정적으로
**서버가 통째로 죽어도 알려준다.** 서버 안에서 도는 감시는 서버가 죽으면
같이 죽어서 침묵한다.

**배포하면 제일 먼저 붙일 것.**

### ② 서버 측 알림 (보조)

cron으로 `health_service.check_kysing()`을 주기 실행하고,
실패 시 Discord/Slack 웹훅으로 발송한다.

- manana 문의용으로 Discord를 이미 쓰므로 웹훅 하나 파면 된다
- 작업량 30분 내외
- 한계: 서버가 죽으면 알림도 안 온다 → ①과 함께 써야 의미가 있다

대안으로 Sentry를 붙이면 `logger.error`가 그대로 알림이 된다
(파서 실패 시 이미 `logger.error`로 찍고 있다).

### 순서

**① → 필요하면 ②.** ①이 커버 범위가 넓고 공짜인데 배포가 선행되어야 한다.
지금은 localhost라 외부에서 찌를 수 없다.

---

## 11. 검색 표기 지원 범위 (실측, 2026-08-05)

같은 곡을 여러 표기로 찾아본 결과다. **로마자는 단어 단위로만 된다.**

| 표기 | 금영 | TJ | manana |
| --- | --- | --- | --- |
| 일본어 원문 (`夜に駆ける`) | ✅ | ✅ | ✅ |
| 한글 발음 (`요루니카케루`, `요루시카`) | ✅ | ✅ | ❌ |
| 로마자 단어 (`kakeru`, `miku`, `yoru`) | ✅ | 일부 | ❌ |
| 로마자 문장 (`yoru ni kakeru`) | ❌ | ❌ | ❌ |
| 한국어 번역 제목 (`밤을 달리다`) | ❌ | ❌ | ❌ |

- **manana에 한글 발음이 없다**는 것이 TJ도 공식으로 전환한 이유다.
- 커버리지는 곡마다 들쭉날쭉하다 (`하나니보레이`는 금영만, `아이도루`는 TJ만).
  **양쪽을 동시에 조회하므로 합집합이 된다** — 우리 구조가 유리한 지점.

### 한국어 번역 제목으로 검색하려면 (미착수)

검색어를 번역해서 던지는 방식은 안 된다. `밤을 달리다` → `夜を走る`로 번역돼도
실제 등록 제목은 `夜に駆ける`다. 의역된 제목이 많아 더 안 맞는다.

방향을 뒤집어야 한다 — **카탈로그 제목을 미리 번역해 인덱싱**하는 것이다.
자체 DB(MySQL)가 있어야 가능하므로 A 작업 이후로 미룬다.

> **2026-08-19 — 전제 조건은 해소됐다.** 자체 DB에 8.6만 곡이 들어 있다.
> 여전히 미착수이며, `/api/translate`는 검색이 아니라 제목 표시용이다 (36번 0항).
같은 방식으로 로마자 필드도 만들 수 있다 (가나→로마자는 규칙 기반이라 번역보다 정확).

대안: iTunes/MusicBrainz로 곡을 특정한 뒤 정식 일본어 제목을 얻어 재검색하는
방법도 있다. 이미 iTunes Search를 쓰고 있어 재료는 있다.

---

## 12. 가사 검색은 금영 전용 (TJ는 역조회로 보완)

`GET /api/search?q={가사}&type=lyrics`

가사 인덱스는 **금영 공식에만 있다.** TJ 공식 검색 옵션에는 가사가 없다
(곡제목/가수/작사가/작곡가/곡번호/메들리뿐).

그래서 이렇게 푼다:

```
1. 금영 가사 검색     '眠れない' → 금영 15곡
2. 각 제목으로 TJ 조회  '怪獣の花唄' → TJ 52988
3. 그룹핑이 두 브랜드를 한 곡으로 묶음
```

실측: 22건 (금영 15 + TJ 7), 양쪽 매칭 6곡, 22초.

주의할 점:

- **느리다.** 금영 가사 검색만 11초 걸린다(곡명 검색은 2초). 금영 서버가
  전문을 훑기 때문이다. `KYSING_LYRICS_TIMEOUT`을 25초로 따로 뒀다 —
  일반 타임아웃(10초)을 쓰면 항상 실패한다.
- TJ 역조회는 곡당 한 번씩 부르므로 **6개 스레드로 병렬 처리**한다
  (순차로는 40초, 병렬로 22초). 상한은 15곡.
- **금영이 간헐적으로 빈 결과를 준다.** 같은 요청을 다시 보내면 정상이라
  첫 페이지가 비면 한 번 더 시도한다.
- 캐시되므로 두 번째부터는 0.01초다.

---

## 13. Apple Music 보관함 연동 (아이디어, 미착수)

**사용자의 Apple Music 보관함/플레이리스트를 읽어 노래방 번호 목록을 자동 생성.**

"내 J-pop 플레이리스트 → 태진·금영 번호 시트"는 경쟁 앱에 없는 기능으로 보인다.
즐겨찾기 자동 채우기에도 쓸 수 있다.

필요한 것:

- **Apple Developer Program $99/년** (필수, 우회 불가)
- MusicKit 토큰 발급 + 사용자의 Apple Music 로그인 및 권한 승인
- 사용자가 **Apple Music 구독자**여야 함 (무료 계정은 라이브러리 접근 불가)

iOS 앱을 출시하려면 어차피 개발자 계정이 필요하므로, **출시 확정 후에는
추가 비용이 없다.** 그때 붙이는 것이 합리적이다.

참고: 지금 쓰는 iTunes Search API는 키가 필요 없지만 **검색만** 된다.
사용자별 라이브러리 접근은 MusicKit이라야 한다.

---

## 14. 자체 카탈로그 DB (적용됨, 2026-08-05)

검색할 때마다 외부 사이트를 치면 **4.7초**가 걸렸다. 금영이 페이지당 2초씩
쓰는 게 병목이라 병렬화로도 못 줄였다. 크롤링해서 우리 DB에 쌓기로 했다.

### 결과

| | 이전 | 지금 |
| --- | --- | --- |
| 검색 (캐시 미스) | 4.7초 | **0.03초** |
| DB 용량 | — | **43MB** (8.6만 곡) |

우려했던 "서버가 무거워진다"는 기우였다. 43MB는 Redis 캐시보다도 작다.
**오히려 가벼워졌다** — 검색마다 남의 서버를 치지 않으니까.

### 열거 전략

카탈로그 전체를 어떻게 훑느냐가 핵심이었다.

1. **월별 백필** — `GET api.manana.kr/karaoke/release/{YYYYMM}/{brand}.json`
   한 번 호출에 그 달 신곡이 통째로 온다. 634개월 × 2브랜드로 8.5만 곡을 받았다.
   가장 싸고 안전한 방법이라 과거 카탈로그는 전부 이걸로 채웠다.
2. **금영 최신 보완** — `kysing.kr/latest/` (이달의 신곡, `s_page` 페이지네이션)
   manana의 금영이 2026-03에서 멈춰 있어 월별 백필로는 못 채운다.
   이걸로 158곡을 더 받아 **금영 최신이 2026-10까지** 왔다.

### 운영

```bash
docker compose exec server flask --app run crawl-backfill   # 최초 1회
docker compose exec server flask --app run crawl-daily      # 매일
docker compose exec server flask --app run crawl-status     # 현황
```

- `crawl_log` 테이블에 월별 성공 여부를 남겨 **중단해도 이어서 받는다.**
- 검색은 DB → 결과 없거나 DB 장애면 기존 외부 조회로 폴백한다.
  `DB_SEARCH_ENABLED=false`로 끄면 예전 동작 그대로다.
- **크롤러가 깨져도 이미 쌓인 DB로 서비스는 계속된다.** 실시간 조회보다
  안정적인 가장 큰 이유다.

### DB로 다 못 채우는 것과, 그 해법

**한글 발음·로마자 컬럼은 만들 수 없다.** 금영·TJ가 그 데이터를
**검색 인덱스로만 갖고 있고 화면에 노출하지 않기 때문이다.**
`요루시카`로 검색하면 결과가 나오지만, 결과 어디에도 "요루시카"라는
문자열이 없다. 크롤링해도 가져올 수 없다.

직접 만들려면 가나→한글 변환기가 필요한데, `夜に駆ける`처럼 한자가 섞이면
읽는 법을 알아야 해서 형태소 분석까지 가야 한다. 별도 프로젝트 규모다.

그래서 **DB가 못 찾으면 공식에 물어보는** 구조로 갔다.

```
DB 조회 (0.03초)
 ├ 결과 3건 이상 → 그대로 응답            일본어 원문 검색
 └ 결과 부족    → 공식 사이트 보강 (4초)   한글 발음·로마자·미크롤링 신곡
                  실패 시 manana 폴백
```

실측:

| 검색어 | 경로 | 시간 | 결과 |
| --- | --- | --- | --- |
| `ヨルシカ` | DB만 | **0.03초** | 39곡 |
| `요루시카` | DB(0) → 공식 | 4.2초 | 53곡 |
| `요루니카케루` | DB(0) → 공식 | 2.2초 | 2곡 (양쪽 번호) |
| `kakeru` | DB(0) → 공식 | 2.7초 | 4곡 |

DB 결과가 넉넉하면 공식을 부르지 않으므로, 흔한 검색은 남의 서버를 안 친다.

### 금영 2024~2026 공백 (남음)

월별 백필은 manana에 의존하는데 그 구간이 비어 있다.
**금영 공식에는 "전곡 목록"이 없어서** 전량 열거가 불가능하다 — 확인한 것:

| 시도 | 결과 |
| --- | --- |
| 곡번호 검색(`category=1`)으로 `5`, `57` 부분 조회 | 0건 — 정확한 번호만 매칭 |
| `/latest/`에 월 파라미터(`ym`, `month`) | 무시됨 — 항상 이번 달 |

곡번호를 하나씩 조회하면 10만 회 × 2초 = 55시간이라 현실성이 없다.

**다만 사용자 입장에서는 위 폴백 구조로 해결된다** — DB에 없어도 공식에서
실시간으로 찾아준다. 느릴 뿐이다. DB 완전성만 남은 문제다.
장기적으로는 DB에 있는 가수 목록으로 금영 공식을 훑어 채우면 된다.

### 발매 예정곡은 받지 않는다

금영은 **발매 예정월**을 미리 올려둔다. 8월에 `2026-10` 곡이 142건 있었다.
번호를 눌러도 기계에 곡이 없으므로 적재 시 걸러내고 기존 것도 지웠다
(`_drop_unreleased`). 참고로 이 곡들은 금영 공식 검색에도 나온다.

### 크롤링 속도 제한

처음엔 넣지 않았다(동시 4개, 무지연). 남의 서버이므로 낮췄다.

- 동시 요청 2개, 요청 사이 0.4초 간격 (`_wait_turn`)
- 백필은 한 번만 돌리고 이후에는 하루 몇 건이라 급할 이유가 없다

### 카테고리 분류

DB가 생겼으니 보컬로이드/애니/게임 태깅이 이제 가능하다. 아직 미착수.

---

## 15. 크롤링 자동 실행 (배포 시 할 일)

**상태:** 미착수. `flask --app run crawl-daily` 명령은 있으나
**아무것도 주기적으로 실행하지 않는다.** 지금은 사람이 직접 쳐야 한다.

로컬에 cron을 걸어도 노트북이 꺼지면 안 돌기 때문에 배포 후에 붙인다.
10번(장애 알림)과 같은 성격이다.

선택지:

| 방법 | 설명 |
| --- | --- |
| 호스트 cron | 배포 서버에서 `docker compose exec server flask --app run crawl-daily` |
| cron 컨테이너 | docker-compose에 스케줄러 컨테이너를 하나 추가 |
| 앱 내 스케줄러 | APScheduler 등. 서버가 재시작되면 같이 죽는 점에 주의 |

하루 1회면 충분하다. manana가 하루 1회 갱신되고 금영 신곡도 그 정도다.
크롤러에는 이미 속도 제한(동시 2, 요청 간 0.4초)이 걸려 있다.

---

## 16. 자체 DB가 불완전한데 완전하다고 가정한다 (해결됨)

**증상:** DB 결과가 3건 이상이면 공식을 건너뛰는데, 그 DB가 불완전했다.

| 가수 | 우리 응답(DB) | 공식 실제 | 누락 |
| --- | --- | --- | --- |
| ヨルシカ | 금영 10 / TJ 29 | 금영 20 / TJ 34 | 15곡 |
| 米津玄師 | 금영 15 / TJ 35 | 금영 30 / TJ 39 | 19곡 |
| **Ado** | **금영 5** / TJ 45 | **금영 22** / TJ 45 | **17곡** |

⚠️ **이 표의 원래 수치는 부풀려져 있었다** (Ado 금영 99곡 → 실제 22곡).
검색 건수를 그대로 센 탓에 1번의 부분일치 노이즈가 섞였다. 2026-08-19에
가수명 정확 일치 기준으로 다시 셌다. TJ 열은 재실측하지 않았다 —
manana TJ의 `Ado` 응답 70건 중 정확 일치는 33건이라 **여기도 같은 오염이 있다.**

원인은 금영 카탈로그의 공백이다. 월별 백필을 manana로 했는데
manana의 금영 수집이 2024년부터 무너져 있다.

```
금영 연도별 적재량
  2021: 1,683곡   2024:   365곡  ← 급감
  2022: 1,819곡   2025:     5곡  ← 사실상 없음
  2023: 1,771곡   2026:   116곡
```

TJ는 manana가 잘 따라가고 있어 문제가 없다 (45 vs 45).

**`DB_ENOUGH = 3`이라는 기준 자체가 틀렸다.**
"몇 건 있으면 완전하다"는 가정이 성립하지 않는다.

### 대응 (구현됨)

공백 메우기 크롤을 돌려 금영 카탈로그를 채웠다.
**Ado 금영 5곡 → 69곡** (2026-08-08 실측, DB만으로).
단 이 69곡은 **가수명을 안 거른 수치다.** 공식에서 `Ado`로 검색되는 99건을
그대로 담았으므로 실제 Ado 곡은 22곡이고 나머지는 노이즈다 (1번 참고).

그리고 DB를 완전하다고 믿지 않도록 **2단계 검색**을 넣었다.

```
1차: GET /api/search?q=Ado        → DB 5곡 + complete:false   (0.03초)
2차: GET /api/search?q=Ado&full=1 → 공식 병합 99건 + complete:true  (4초)
                                    (99건 중 실제 Ado 곡은 22곡 — 1번 노이즈)
```

SSE 없이 클라이언트가 두 번 부르는 것으로 충분하다.

**주의: 목록이 흔들린다.** 발매순 정렬이라 나중에 온 곡이 위로 끼어들면
사용자가 카드를 누르는 순간 밀려 엉뚱한 것을 누를 수 있다.
새로 온 것은 아래에만 붙이거나, "N곡 더 찾았어요" 배너를 띄우고
누르면 반영하는 방식이 안전하다.

**이것은 증상 완화지 근본 해결이 아니다.** 금영 공백이 남는 한 매 검색마다
공식을 치게 된다. 병행해서 **금영 공백 메우기**가 필요하다 —
DB에 있는 금영 가수 목록으로 공식을 훑으면 채울 수 있다
(공식은 전곡 열거가 안 되지만 가수별 검색은 된다).

---

## 17. 공식 사이트를 언제 찌르는가 (2026-08-05 개선)

처음엔 1차 응답을 **무조건** `complete: false`로 내보냈다. 그러면 검색마다
공식을 치게 되어 **자체 DB를 만든 의미가 없어진다**(빨리 몇 개 먼저 보이는 것 말고는).

기준을 바꿨다. **DB가 원리적으로 답할 수 없는 경우에만** 공식을 찌른다.

| 검색어 | 공식 호출 | 이유 |
| --- | --- | --- |
| 일본어 + 결과 있음 (`ヨルシカ`) | **안 함** | DB에 그 표기가 그대로 있다 |
| 일본어 + 0건 (`メズマライザー`) | 함 | 아직 크롤링 안 된 신곡일 수 있다 |
| 한글 발음 (`요루시카`) | 함 | 한글 발음은 공식 검색 인덱스에만 있다 |
| 로마자 (`kakeru`) | 함 | 위와 같다 |

실측: `ヨルシカ` 검색 시 API 요청 **1건**(DB만), 카드 37장이 0.03초에 나온다.

---

## 18. 공식 사이트 검색의 신뢰도 문제 (업스트림, 조치 불가)

사용자 제보로 발견했다. **금영 곡명 검색이 긴 검색어에서 실패한다.**

```
금영 가수검색 'miku'          → 115곡 (メズマライザー 57747 포함) ✅
금영 곡명검색 'メズマライザー'    → 0건  ❌
금영 곡명검색 'メズマ'          → 1건  ✅
TJ  가수검색 'miku'          → 0건 (TJ는 로마자 미지원)
```

같은 곡이 가수 검색으로는 나오고 곡명 검색으로는 안 나온다.
브랜드마다 지원하는 표기도 달라서(TJ는 로마자 없음, 금영은 있음)
"어떤 검색어로 치느냐"에 따라 한쪽 브랜드만 나오는 일이 생긴다.

**자체 DB가 이 문제를 푼다.** 우리 DB는 정규화 후 부분 일치(LIKE)로 찾으므로
검색어 길이와 무관하고 브랜드 편차도 없다. 크롤링이 끝날수록 이 증상은 줄어든다.

---

## 19. 브랜드 탭은 서버를 다시 부르지 않는다

'전체'로 받아온 결과에 두 브랜드가 다 들어 있으므로 화면에서 거르기만 한다
(`filterByBrand`). 탭을 누를 때마다 재조회하면 4초를 또 기다리게 된다.

`useSearch`는 브랜드를 인자로 받지 않고 항상 `all`로 조회한다.
실측: 태진/금영 탭 전환 시 **추가 요청 0건**.

주의: 검색 결과 수는 전체 탭에서만 서버 총계를 쓴다. 브랜드를 거르면
서버 총계와 맞지 않으므로 화면에 있는 카드 수를 센다.

---

## 20. '결과 없음'을 성급하게 띄우지 않는다

1차(DB) 응답이 0건이어도 공식 조회가 남아 있으면 "검색 결과가 없어요"를
띄우면 안 된다. 잠시 뒤 결과가 나타나 사용자를 혼란스럽게 한다.

`loadingMore2`(공식 조회 중)일 때는 대신 스피너를 보여준다.

**스피너는 결과 목록 블록 바깥에 둬야 한다.** 안에 두면 결과가 0건일 때
목록 블록 자체가 렌더되지 않아 '없음'도 스피너도 안 나오는 빈 화면이 된다.
(실제로 그렇게 만들었다가 잡았다)

---

## 21. 항상 공식을 찌르면(D안) 생기는 비용

"매번 공식을 보면 정확하지 않나"에 대한 실측이다.
**검색 1회에 남의 서버로 6~13회 요청이 나간다** (페이지네이션 때문).

| 검색어 | 외부 요청 | 소요 |
| --- | --- | --- |
| `ヨルシカ` | 6회 | 4.2초 |
| `米津玄師` | 9회 | 7.0초 |
| `Ado` | 13회 | 8.2초 |

속도 말고도 이런 것들이 걸린다.

1. **IP 차단 위험 (가장 큼)** — 캐시 덕에 같은 검색어는 하루 1번이지만,
   서로 다른 검색어는 누적된다. 사용자 1,000명이면 하루 2.4만 요청,
   10,000명이면 24만 요청이다. 한 IP에서 나가는 양으로는 눈에 띈다.
   차단당하면 폴백(manana)으로 내려가 공식을 쓴 이유가 사라진다.
2. **서버 동시성** — 요청 하나가 4~8초 동안 워커를 점유한다.
   DB 조회는 워커 1개로 초당 30건, 공식 조회는 초당 0.15건이다.
   동시 사용자 10명을 감당하려면 워커가 수십 개 필요하다.
3. **장애 노출면** — 공식이 흔들리면 모든 검색이 영향받는다.
   DB 우선이면 일부만 느려지고 나머지는 멀쩡하다.

Redis 캐시 용량은 소스와 무관하므로 문제되지 않는다.

**결론: A(더 찾아보기 버튼) + C(가수별 훑기)** 로 간다.
D의 유일한 장점인 "항상 최신"은 매일 크롤링으로 대부분 확보되고,
남는 차이(오늘 등록된 신곡)는 버튼으로 사용자가 직접 확인한다.

---

## 22. 공백 메우기 대상 선정 (버그 수정)

처음엔 **가수 이름에 일본어가 있는 가수**만 훑었다(1,129명).
그러면 **Ado, YOASOBI, Vaundy, Aimer, LISA, AKB48처럼 로마자 이름을 쓰는
일본 가수가 통째로 빠진다.** 사용자 제보로 발견했다.

곡 제목에 일본어가 있으면 그 가수도 포함하도록 고쳤다 → **1,681명**.

```sql
WHERE singer REGEXP '[ぁ-んァ-ヶ一-龥]'
   OR title  REGEXP '[ぁ-んァ-ヶ一-龥]'
```

빠져 있던 대표 가수: AKB48(38곡), Ado(30), Aimer(23), back number(23),
Vaundy(23), Mrs. Green Apple(22), LISA(20), YOASOBI(20).

---

## 23. 한국곡 노이즈 필터

`미쿠`로 검색하면 드라마 '미미쿠스' OST 같은 한국곡이 딸려 온다.
노래방 DB가 부분 문자열로 매칭하기 때문이다. 일본곡 전용 앱이므로 감춘다.

**보수적으로 판단한다** — 한글이 있고 **일본어가 전혀 없을 때만** 한국곡으로 본다.

확인한 것 (2026-08-05):
- 한국어 제목 + 일본어 가수명인 곡: **0건** → 오탐 위험 낮음
- 제목에 일본어가 섞이면 남는다 (`残酷な天使のテーゼ (에반게리온 OP)`)
- 로마자만 있는 일본곡은 한글이 없으니 안 걸린다 (`Idol` / `YOASOBI`)

걸러지는 것: 애니 주제가의 한국어 더빙판 (예: 코요태 '우리의꿈(원피스 OP)').
이건 실제로 한국곡이라 감춰도 무방하다고 판단했다.
원곡을 부르고 싶은 사람이 대상이기 때문이다.

실측: `미쿠` 검색 7곡 → **5곡** (한국곡 2건 제거)

### 제목과 가수를 각각 본다 (2026-08-19 수정)

처음에는 제목과 가수를 **붙여서 한 덩어리로** 판정했다. 그러면 둘 중
하나만 일본어여도 통과한다 — **제목이 일본어인 한국 가수 곡**이 그렇게
새어 들어왔다 (아이유 `サクラ` 같은 일본 활동곡·커버).

이제 둘을 따로 보고 **하나라도 한글 전용이면** 한국곡으로 본다.

| 제목 | 가수 | 이전 | 지금 |
| --- | --- | --- | --- |
| `サクラ` | `いきものがかり` | 남음 | 남음 |
| `サクラ` | `아이유` | **남음** | 빠짐 |
| `우리의꿈(원피스 OP)` | `코요태` | 빠짐 | 빠짐 |
| `残酷な天使のテーゼ (에반게리온 OP)` | `高橋洋子` | 남음 | 남음 |

**오탐 위험은 실측했다 (2026-08-19).** 이론상 '일본 가수를 한글로 등록한
곡'이 함께 걸릴 수 있어 확인했다. 금영·태진 공식에 일본 가수 8팀을 한글
발음으로 조회해 가수명이 한글 전용인 행을 셌다.

| 검색어 | 금영 | 태진 | 한글 전용 가수 |
| --- | --- | --- | --- |
| 요아소비 | 27 | 31 | 0 |
| 요루시카 | 20 | 33 | 0 |
| 하츠네미쿠 | 107 | 14 | 0 |
| 미쿠 | 115 | 0 | 0 |
| 아이묭 | 24 | 33 | 0 |
| 오피셜히게단디즘 | 18 | 32 | 0 |
| 백넘버 | 15 | 23 | 0 |
| 킹누 | 14 | 22 | 0 |

**한 건도 없었다.** 한글 전용 가수명이 나온 것은 `아도` 검색뿐이고
(`아도니스`, `ADORA(Feat.나띠(KISS OF LIFE))`, `검정치마`) 전부 실제
한국 가수다 — 걸러져야 할 쪽이다.

일본 노래방은 일본 가수를 일본어로 등록하는 것이 원칙이라 앞으로도
드물 것으로 본다. 생기면 설정의 '한국곡 포함'을 켜면 보인다.

---

## 24. '더 찾아보기' 버튼

DB가 완전하지 않을 수 있다는 것을 인정하고, 사용자에게 확인 수단을 준다.

목록 끝에 "찾는 곡이 없나요? 노래방 사이트에서 더 찾아보기"를 띄운다.
누르면 공식까지 뒤져 결과를 아래에 덧붙인다(4초). 자동 호출이 아니라
**사용자가 원할 때만** 남의 서버를 쓴다.

크롤링을 매일 돌려도 "오늘 등록된 신곡"은 내일까지 DB에 없다.
그 간극을 메우는 장치다.

---

## 25. TJ 강조 태그가 이름을 쪼갠다 (버그 수정)

TJ는 검색어와 일치한 부분을 `<span class='highlight'>`로 감싼다.
파서가 모든 태그를 공백으로 바꾸고 있어서 이름이 조각났다.

```
원본: Do As <span class='highlight'>Infinity</span>
잘못: Do As Inf i n i ty     ← 태그를 공백으로 치환
수정: Do As Infinity         ← 인라인 태그는 지우고 나머지만 공백
```

`span/b/strong/em/i`는 지우고 나머지 태그만 공백으로 바꾸도록 고쳤다.

**피해 범위는 작았다.** 가수 이름 전체로 검색하면 통째로 하이라이트되어
쪼개지지 않기 때문이다. 실제 손상은 `L'Arc~en~Ciel` 하나(59곡)뿐이었고
`L'Arc~ en ~Ciel`로 저장돼 있었다. 재수집과 직접 교정으로 0건이 됐다.

씨앗 문자('a', 'あ' 등)로 훑는 방식을 쓰면 이 버그가 대량으로 터진다.
금영은 강조 태그를 쓰지 않아 영향이 없다.

---

## 26. TJ의 일본곡 필터로 전량 열거는 안 된다

`nationType=JPN`으로 일본곡만 거를 수 있지만, **검색어 없이는 0건**이다.

| 시도 | 결과 |
| --- | --- |
| 빈 검색어 + JPN (strType 0/1/2) | 0건 |
| `/song/recent_song` + JPN | 0건 (서버 렌더링 안 됨) |

씨앗 문자를 넣으면 나오지만(예: 가수명 'a' → 15건/페이지),
결과는 **곡** 단위라 가수를 모으려면 많은 페이지를 넘겨야 한다.
'a' 씨앗 5페이지(75곡)에서 얻은 고유 가수는 9명뿐이었다.

전량 열거 수단이 아니라 **모르는 가수를 발견하는 보조 수단**으로는 쓸 만하다.
다만 25번 버그를 먼저 고쳐야 한다(고쳤다).

---

## 27. 인증 (구현됨, 2026-08-06)

아이디 + 비밀번호만 받는다. 이메일은 비밀번호 찾기를 붙일 때 선택 항목으로
되살린다 (`users.email`을 NULL 허용으로 바꿔 자리는 남겨 뒀다).

```
POST   /api/auth/register  {username, password}
POST   /api/auth/login     {username, password}
POST   /api/auth/logout
GET    /api/auth/me        로그인 안 했으면 null (401 아님)
PATCH  /api/auth/me        {username?, avatar?}
```

실패 응답에 **`field`를 실어 보낸다**. 클라이언트가 어느 입력창 아래에
메시지를 붙일지 알아야 하기 때문이다.

```json
{ "error": "invalid_request", "field": "username", "message": "중복된 아이디예요." }
```

### 적용한 보안

| 항목 | 내용 |
| --- | --- |
| **bcrypt 해싱** | 작업계수 12 (`BCRYPT_ROUNDS`). 솔트가 매번 달라 같은 비밀번호도 다른 해시가 된다 |
| **HTTPS 준비** | `SESSION_COOKIE_SECURE`가 운영 설정에서 켜진다. 개발은 끔 |
| **로그인 시도 제한** | 계정당 5회 실패 → 15분 잠금. 카운터는 Redis |
| **httpOnly 쿠키** | Flask 서명 세션. 자바스크립트가 못 읽어 XSS로 새지 않는다. SameSite=Lax |
| **사용자 열거 방지** | 없는 아이디와 틀린 비밀번호가 **같은 메시지**를 낸다 |
| **아이디 대소문자 무시** | `Jihyeon`/`jihyeon`이 따로 생기면 서로를 사칭할 수 있다 |
| **경쟁 상태 방어** | 중복 확인과 INSERT 사이를 UNIQUE 제약이 막는다 (`INSERT IGNORE`) |

**왜 신경 쓰는가** — 우리 데이터(즐겨찾기 목록)는 값어치가 없다. 문제는
**사람들이 비밀번호를 돌려쓴다**는 것이다. 우리 DB가 새면 그 조합이 다른
사이트에서 시도된다. 피해는 우리가 아니라 사용자가 본다.

비밀번호 복잡도(대문자·특수문자)는 강요하지 않는다. 길이만 본다 —
현대 권고에 맞고, 강요하면 오히려 예측 가능한 비밀번호를 쓰게 된다.

### 아이디 중복검사

**가입 버튼을 눌렀을 때만** 확인한다. 실시간 확인 API를 두면 아이디 목록을
긁어갈 수 있다. 다만 가입 시점에도 "중복된 아이디"라고 답하는 이상 열거를
완전히 막지는 못한다. 필요해지면 rate limit을 걸면 된다.

### 아직 없는 것

- **비밀번호 찾기** — 이메일이 없어 복구 수단이 없다
- **rate limit** — 가입·중복확인에는 아직 제한이 없다 (로그인에만 있음)
- **즐겨찾기 서버 저장** — 로그인해도 즐겨찾기는 여전히 기기 로컬이다

---

## 28. IP 단위 요청 제한

계정당 로그인 5회 제한(27번)과 **역할이 다르다.**
그쪽은 '이 계정을 노린 대입'을 막고, 이쪽은 '한 곳에서 쏟아내는 요청'을 막는다.
아이디를 바꿔가며 시도하면 계정 카운터는 오르지 않으므로 둘 다 필요하다.

| 대상 | 제한 |
| --- | --- |
| `POST /api/auth/register` | 1시간에 10회 |
| `POST /api/auth/login` | 1분에 20회 |
| `PATCH /api/auth/me` | 1분에 20회 |
| `POST /api/favorites` | 1분에 60회 |

**Redis가 없으면 통과시킨다(fail open).** 캐시 장애로 로그인이 막히는 것보다는
잠시 제한이 풀리는 편이 낫다고 봤다. fail closed로 하면 Redis가 죽는 순간
서비스 전체가 멈춘다.

초과하면 429와 `Retry-After` 헤더를 준다.

⚠️ **`X-Forwarded-For`는 위조할 수 있다.** 배포 시 리버스 프록시가 이 헤더를
덮어쓰도록 설정해야 한다. 안 그러면 헤더만 바꿔가며 제한을 우회할 수 있다.

---

## 29. 설정 화면 (구현됨, 2026-08-06)

| 묶음 | 항목 |
| --- | --- |
| 검색 | 한국어 노래 표시 토글, 정렬 기준(최신 발매순 / 곡번호순) |
| 데이터 | 즐겨찾기 내보내기(클립보드), 즐겨찾기 전체 삭제 |
| 정보 | 앱 버전, 노래 정보 출처, 글꼴, 문의, 라이선스 문구 |
| — | 회원탈퇴 (맨 아래, 로그인 상태에서만) |

설정은 **계정이 아니라 기기에 저장한다**(`localStorage`). 기기마다 다르게
쓰고 싶을 수 있고(집은 전체, 폰은 자주 가는 노래방 브랜드), 로그인 없이도
유지돼야 하기 때문이다.

### 서버에 추가한 것

```
GET    /api/search?sort=release|no&korean=1
DELETE /api/favorites          담아둔 곡 전부 삭제
DELETE /api/auth/me            회원 탈퇴
```

- **정렬은 Redis 캐시 키에 넣지 않는다.** 캐시에는 곡 목록만 담고,
  정렬은 응답을 만들기 직전에 적용한다. 키에 넣으면 내용이 같고 순서만
  다른 목록을 두 벌 저장하게 된다.
  한국곡 포함 여부는 목록에 들어가는 곡 자체가 달라지므로 키를 나눈다.

  (사용자가 고른 설정은 별개로 브라우저에 저장되어 앱을 껐다 켜도 유지된다.
   캐시 키 얘기는 서버가 검색 결과를 저장하는 방식일 뿐이다.)
- 곡번호순은 **숫자로 비교한다.** 번호가 문자열이라 그냥 정렬하면
  9가 10보다 뒤로 간다.
- 회원 탈퇴 시 즐겨찾기·검색이력은 FK `ON DELETE CASCADE`로 함께 사라진다.

### 넣지 않은 것

- **캐시 비우기** — 검색 캐시는 서버(Redis)에 있어 사용자가 지울 게 없다.
  버튼만 있고 아무 일도 안 하면 오히려 혼란스럽다.
- **다크모드** — 아이보리 손그림 톤에 맞춰 설계돼 있어 색만 뒤집을 수 없다.
  팔레트를 새로 짜야 해서 작업량이 크다. 보류.
- **개인정보처리방침·이용약관** — 출시 전 필수. 링크 자리에 TODO를 남겼다.

### 곁들여 고친 것

탭을 옮기면 검색어가 사라졌다. 설정에서 토글을 바꾸고 홈으로 돌아와
결과를 확인하는 흐름이 깨지므로, 검색 상태를 `App`으로 올렸다.

---

## 30. 가사보기 — 금영에서 긁고, 없으면 검색으로 넘긴다 (구현됨, 2026-08-06)

> ⚠️ **권리 확인이 끝나지 않았다.** 37번을 함께 볼 것.

### Musixmatch를 버린 이유

원래 Musixmatch를 붙였는데 (8번 표) 무료 플랜이 **앞 30%만** 준다.
노래방에서 따라 부르려고 여는 화면인데 후렴 전에 끊긴다.

그러다 금영 검색 결과 HTML 안에 **가사 전문이 통째로 들어 있는 걸** 발견했다.
곡명 칸(`cells[2]`)의 `<div class="LyricsCont">`다. 별도 요청도, 키도, 요금도
없고 **한글 발음까지 붙어 온다** — 일본어를 못 읽어도 부를 수 있으니
이 서비스에는 가사 전문 API보다 낫다. Musixmatch 설정은 걷어냈다.

### 세 줄 묶음 파싱

금영은 한 구절을 세 줄로 내보낸다:

```
싱그루 사이즈노 헤야데      ← 한글 발음
//////// へ/や /           ← 후리가나 (루비 위치가 빠져 못 씀)
シングルサイズの部屋で       ← 일본어 원문
```

**처음엔 3줄씩 끊었는데 틀렸다.** `ミスター`처럼 'La la la' 도입부로 시작하는
곡은 발음 줄이 없어서 그 뒤가 통째로 한 칸씩 밀린다. 발음과 원문이 엇갈린
가사가 화면에 나갔다.

그래서 줄 수를 세지 않고 **줄마다 무엇인지 보고 짝짓는다**:

| 판별 | 처리 |
| --- | --- |
| 한글이 있다 | 발음. 들고 있다가 다음 원문과 짝짓는다 |
| 히라가나·공백·`/`뿐이고 **`/`가 하나는 있다** | 후리가나. 버린다 |
| 나머지 | 원문 |

`/` 조건이 핵심이다. 이게 없으면 `ずっと ずっと ずっと` 같은 **히라가나뿐인
가사 원문이 후리가나로 오인되어 통째로 사라진다** (`Pale Blue`에서 86줄 중
28줄이 날아갔다).

실측: YOASOBI·ヨルシカ·米津玄師 76곡 전부 파싱 성공, 어긋난 곡 없음.

### 조회 경로

가수 검색을 먼저 쓴다. 금영 곡명 검색이 **긴 일본어 제목에서 0건을 돌려주기
때문이다** (실측: `夜に駆ける` 0건, `YOASOBI` 15건 — 18번과 같은 원인).
가수로 최대 4페이지(60곡)를 훑고, 못 찾으면 곡명으로 한 번 더 본다.

제목 매칭은 `normalize_text`를 쓴다. 금영 제목에는 `怪物 ("BEASTARS"OP)`처럼
꼬리표가 붙는데 괄호를 떼는 규칙이 이걸 흡수한다.

**느리다 — 실측 5.0초** (페이지당 2초 남짓, 2페이지까지 훑은 경우).
그래서 카드를 눌렀을 때만 부르고, Redis에 30일 캐시한다. 캐시가 있으면 0.2초다.
없다고 판정한 것도 캐시하되(매번 5초를 다시 쓸 순 없다) 나중에 금영에 실릴 수
있으니 24시간만 잡는다.

### 못 찾으면 구글 검색으로 넘긴다

**태진 전용 곡(일본곡의 41%)은 가사를 구할 데가 없다.** 태진은 검색 HTML에도,
곡 상세 페이지에도, `/song/accompaniment`에도 가사를 두지 않는다 (12번에서 확인).
금영에 있는 곡 중에도 가사 팝업이 비어 있는 것들이 있다 (`好きだ`, `UNDEAD`).

이런 곡은 `"{제목}" {가수} 가사`로 구글 검색 링크를 만들어 내보낸다.
서버가 `search_url`을 **항상** 채워 주므로 클라이언트는 조건 없이 띄우면 된다.

**미리 매핑해 둘 필요가 없다** — 누를 때 주소만 조립하면 되고, 우리 서버는
아무것도 조회하지 않는다. 실패할 여지도 비용도 없다.

**보카로 가사 위키를 직접 걸지 않은 이유** (2026-08-06 확인):
1. 검색 폼이 `action="dummy"`인 자바스크립트 방식이라 URL로 못 부른다.
2. 문서 주소가 제목과 무관한 영어 슬러그다 (`/blue-and-the-polar-stars` →
   쪽빛과 극성). `メズマライザー`에서 주소를 유추할 방법이 없어, 전체 문서
   목록을 크롤링해 매핑을 만들어야 한다.
3. 보카로만 덮는다. 태진 전용 3,157곡은 애니송·J-POP이 훨씬 많다.

노력 대비 이득이 적어 구글 검색 한 줄로 갈음했다. 나중에 필요하면 매핑을 얹으면 된다.

---

## 31. 썸네일과 30초 미리듣기 (구현됨, 2026-08-06)

카드 왼쪽에 42px 정사각 칸을 뒀다. **누르기 전에는 ▶만 있는 빈 칸**이고,
누르면 그때 iTunes를 불러 앨범아트를 깔고 30초 프리뷰를 재생한다.

미리 부르지 않는 이유는 가사와 같다 — 목록 한 장이 50카드면 요청도 50번이다.
받아온 결과는 카드가 들고 있어서 다시 누를 때는 요청이 없다.
서버는 7일 캐싱한다(못 찾은 것은 1일).

### 한 번에 한 곡만

카드마다 오디오를 두면 여러 곡이 겹쳐 나온다. 재생기는 **모듈 수준에 하나만**
두고 "지금 울리는 곡이 누구인지"만 `useSyncExternalStore`로 공유한다.
앨범아트는 카드마다 다르니 각자 들고 있는다 (`hooks/usePreview.ts`).

멎어야 하는 순간도 챙긴다:
- 다른 곡의 ▶ → 앞 곡 정지
- 같은 곡 다시 → 정지
- **검색어 변경 → 정지.** 카드가 사라졌는데 소리만 남으면 끌 방법이 없다.

### 정리하다가 다음 곡을 꺼먹은 버그

곡을 이어서 누르면 **두 번째가 재생되자마자 멈췄다.**

앞 곡을 정리할 때 `src=''`로 내려받기를 끊는데, 이게 **error 이벤트를 낸다.**
그 곡에 붙여 둔 `error` 핸들러가 `stopPreview()`를 부르고, 그때는 이미 다음 곡이
현재 재생기로 올라가 있어서 **엉뚱한 곡이 꺼졌다.**

`src`를 비우기 **전에 리스너를 뗀다**(`detach()`). `play()`의 거부 처리도
"그새 다른 곡으로 넘어갔으면 건드리지 않는다"로 막아 뒀다.

실측(Audio 생성자를 가로채 확인):

| 조작 | 고치기 전 | 고친 뒤 |
| --- | --- | --- |
| 0번 재생 | 재생 1 | 재생 1 |
| 1번으로 전환 | **재생 0** | 재생 1 |
| 1번 다시 | **재생 1**(다시 켜짐) | 재생 0 |

### 자동재생 차단

`play()`는 브라우저가 자동재생을 막으면 거부된다. 첫 탭은 조회를 기다리느라
사용자 제스처와 시차가 생겨 특히 걸리기 쉽다.

거부되면 조용히 멈춘 상태로 되돌린다. **응답은 이미 받아 뒀으므로 한 번 더
누르면 기다림 없이 바로 재생된다.** 에러를 띄우는 것보다 낫다.

### 미리듣기가 없는 곡

iTunes에 없으면 버튼을 비활성으로 두고 `aria-label`을 "미리듣기 없음"으로 바꾼다.
가사와 달리 대신 보낼 곳이 없어서 링크는 두지 않았다.

---

## 32. 캐시 키 충돌 — 가사 검색과 가사 조회 (버그 수정, 2026-08-06)

부가 기능 캐시와 검색 캐시가 **같은 이름 공간을 쓰고 있었다.**

```
검색  make_key(search_type, brand, query)   → kada:search:v3:lyrics:all:yoasobi
조회  make_key("lyrics", title, singer)     → kada:search:v3:lyrics:all:yoasobi
```

`search_type`에 `lyrics`가 있고 `brand`에 `all`이 있어서, 이 둘이 정확히 겹친다:

| 요청 | 뜻 |
| --- | --- |
| `/api/search?q=YOASOBI&type=lyrics&brand=all` | 전체 브랜드에서 'YOASOBI' 가사 검색 |
| `/api/lyrics?title=ALL&singer=YOASOBI` | 'ALL'(곡)/YOASOBI의 가사 |

먼저 캐시된 쪽이 다른 쪽에 그대로 나간다. 검색이 가사 응답을 읽으면
`_build_groups`가 엉뚱한 형태를 받아 터지고, 반대면 화면에 가사 대신
검색 결과가 담긴 채 `available`이 없어 "가사를 못 가져왔다"가 뜬다.

`ALL`이라는 제목의 곡은 실제로 있다. 확률이 낮을 뿐 못 일어날 일이 아니다.

**검색 키에 `q` 토막을 앞세워** 이름 공간을 갈랐다
(`kada:search:v3:q:lyrics:all:yoasobi`). 부가 기능 키는 `lyrics`/`preview`/
`translate`로 시작하므로 이제 구조적으로 겹칠 수 없다.
배포하면 기존 검색 캐시는 버려진다 — 다시 채워질 뿐이라 문제없다.

회귀 테스트: `TestCacheNamespace`.

---

## 33. 오늘 함께 고친 것들 (2026-08-06)

### 통신 실패가 버튼을 영영 잠갔다

미리듣기 조회가 실패하면 `available: false`로 뭉뚱그려져 **'이 곡은 미리듣기가
없다'로 굳었다.** 잠깐 끊긴 것뿐인데 버튼이 비활성으로 잠겨 새로고침 전에는
되살릴 수 없었다.

서버는 못 찾아도 200 + `available: false`로 답한다. 그러니 **`!ok`는 프록시·
네트워크 문제뿐**이고, 이건 다시 시도할 만한 실패다. `fetchPreview`/`fetchLyrics`가
그 경우 예외를 던지도록 바꾸고, 훅은 `missing`(다시 눌러도 소용없음)과
`failed`(다시 누르면 재시도)를 나눠 들고 있는다.

### 조회 중 연타하면 요청이 겹쳤다

`toggle`에 `loading` 가드가 없어 3번 누르면 3번 나갔다. 실측으로 1회 확인.

### 같은 제목·가수의 다른 곡이 함께 재생 중으로 보였다

재생 중인 카드를 `제목 가수`로 식별해서, 이 값이 같은 그룹이 둘이면 둘 다
▶가 정지 모양으로 바뀌었다. 그룹의 `match_key`를 쓰도록 바꿨다.

### 가사 조회에 빈 페이지 재시도가 빠져 있었다

`search()`에는 있는 방어인데(금영은 부하가 걸리면 결과 없는 페이지를 간헐적으로
돌려준다) `find_lyrics()`에는 없었다. 이걸 진짜 0건으로 받아들이면
**'가사 없음'이 하루 동안 캐시된다.** 첫 페이지가 비면 한 번 더 보게 했다.

### 잔손질

- `_row_blocks()`로 행 파싱을 모았다. `_parse`와 `find_lyrics`가 같은 코드를
  두 벌 갖고 있었다.
- `lyrics.py`의 `current_app` import를 모듈 상단으로 (함수 안에 있을 이유가 없었다).
- 가수 없이 검색 링크를 만들면 `"제목"  가사`처럼 공백이 두 칸이었다.

---

## 34. 운영용 WSGI 서버 — gunicorn (구현됨, 2026-08-08)

`run.py`가 Flask 개발 서버를 쓰고 있었다. Werkzeug가 직접 "프로덕션에 쓰지
말라"고 찍어 주는 그것이다.

### 왜 급했나

작업 중에 실제로 물렸다. `full=1` 검색이 금영을 훑는 동안 **다른 요청이 몇 분간
막혔다** — 헬스체크도, DB 조회도. 이 서버가 하는 일의 대부분은 계산이 아니라
**남의 서버를 기다리는 것**이라 이 문제가 그대로 드러난다.

gunicorn을 올린 뒤 같은 상황을 재현한 결과:

| 상태 | 스크래핑 중 `/api/health` |
| --- | --- |
| Flask 개발 서버 | 수십 초~응답 없음 |
| gunicorn (gthread 4×8) | **0.003초** |

### 설정 (`server/gunicorn.conf.py`)

- **`worker_class = "gthread"`.** 기본 sync 워커는 남의 서버를 기다리는 동안
  통째로 막힌다. 스레드 워커면 그 사이 다른 요청을 받는다.
  gevent는 몽키패치가 필요해서 쓰지 않았다 — 지금 코드가 `ThreadPoolExecutor`를
  이미 쓰고 있어 스레드 쪽이 자연스럽다.
- **`timeout = 120`.** 기본값 30초로는 부족하다. `full=1` 검색은 금영을 여러
  페이지 훑어서 1분을 넘길 수 있다. 여기서 잘리면 워커가 죽고 502가 나간다.
- **`preload_app = False`.** 포크 전에 앱을 올리면 MySQL 커넥션 풀과 Redis
  클라이언트를 자식들이 나눠 쓰게 된다. 지금은 둘 다 첫 사용 시점에 만들어지지만
  그 가정에 기대지 않는다.
- `forwarded_allow_ips` — 프록시 뒤에 둘 때 요청 제한이 진짜 클라이언트 IP를
  보려면 필요하다 (28번).

### 개발은 그대로

이미지의 기본 CMD만 gunicorn으로 바꾸고, `docker-compose.yml`이 개발용으로
`command: python run.py`를 덮어쓴다. 리로더가 필요한 개발 흐름은 건드리지 않았다.

---

## 35. 클라이언트 테스트 (구현됨, 2026-08-08)

vitest + Testing Library로 45개. 서버가 168개인데 클라이언트가 0개였고,
**하루에 잡은 버그 3개가 전부 클라이언트 쪽이었다.**

| 파일 | 개수 | 무엇을 지키나 |
| --- | --- | --- |
| `utils/groups.test.ts` | 12 | 페이지 병합, 늦게 온 결과를 끝에 붙이기, 브랜드 필터 |
| `hooks/usePreview.test.ts` | 11 | 한 번에 한 곡, 정리 순서, 연타, 실패 후 재시도 |
| `components/SongCard.test.tsx` | 8 | 번호 배치, 즐겨찾기, 누르기 전엔 조회 안 함 |
| `components/LyricsModal.test.tsx` | 7 | 발음·원문 짝, 링크 상시 노출, 실패 시 링크 유지 |
| `utils/api.test.ts` | 7 | "못 찾음"(200)과 "통신 실패"(예외)의 경계 |
| `hooks/useSearch.test.ts` | 2 | 페이지를 같은 조건으로 이어 받기 (38번) |

### 쓰자마자 버그가 하나 더 나왔다

연타 방지를 `loading` 상태로 걸어 뒀는데, **리렌더가 끼어들어야만 동작했다.**
브라우저 클릭은 사이에 리렌더가 나서 통과했지만(그래서 실측으로는 멀쩡해 보였다),
리렌더 없이 연속 호출하면 그대로 뚫렸다. `useRef`로 바꿔 동기적으로 막는다.

**브라우저로 눌러 본 것만으로는 못 잡는 종류였다.**

### jsdom 때문에 손봐야 했던 것

- 오디오 재생 구현이 없다. `HTMLMediaElement.prototype.pause`를 setup에서
  갈아 끼워 "Not implemented" 경고를 없앴다.
- 재생기가 모듈 수준에 하나뿐이라 테스트끼리 샌다. `afterEach`에서 멈추되
  **`act()` 안에서** 해야 한다 — 이 훅의 `afterEach`가 `cleanup`보다 먼저 돌아
  아직 붙어 있는 컴포넌트에 상태 변경이 가기 때문이다.

---

## 36. 검색 규칙 (2026-08-19 정리)

`/api/search` 한 번에 무슨 일이 벌어지는지, 순서대로. 구현은
`app/services/search_service.py`의 `search()` 하나에 모여 있다.

### 0. 무엇으로 검색할 수 있나 (헷갈리기 쉬운 곳)

`type`은 세 가지다.

| type | 무엇을 찾나 | 상태 |
| --- | --- | --- |
| `song` | 곡명 | ✅ |
| `singer` | 가수 | ✅ |
| `lyrics` | 가사 (금영 전용, TJ는 역조회) | ✅ 12번 |

**표기**는 원문·한글 발음·로마자 단어까지 된다 (11번 실측표).
발음 검색은 별도 구현이 아니라 **공식 사이트의 검색 인덱스가 받아주는 것**이라,
아래 4단계에서 공식을 찌르는 조건에 걸려 동작한다.

**번역은 검색에 쓰지 않는다.** `/api/translate`(DeepL)는 곡 제목을 한국어로
**보여주는** 용도다. `search_service`는 이 서비스를 부르지 않고, `songs`
테이블에도 번역 제목 컬럼이 없다. 즉 `밤을 달리다`로는 `夜に駆ける`를 못 찾는다.
(2026-08-19 기준 클라이언트도 이 엔드포인트를 아직 호출하지 않는다.)

이유는 11번에 적었다 — 검색어를 번역해 던지면 의역 때문에 더 안 맞는다.
하려면 **카탈로그 제목을 미리 번역해 인덱싱**해야 하고, 그 전제였던 자체 DB는
이제 있으므로 착수 가능한 상태다 (미착수).

### 1. 검색어를 검사한다

`_validate()` — 검색어가 비었거나 100자를 넘으면 400. 지원하지 않는
브랜드(`joysound`, `dam`)나 타입(`composer` 등)도 400이다.
`type`은 `song`(곡명) / `singer`(가수) / `lyrics`(가사) 셋이다.

### 2. 캐시를 본다

키는 `q2:{타입}:{브랜드}:{정규화한 검색어}`이고 뒤에 `:ko`(한국곡 포함),
`:quick`(1차 검색)이 붙는다. 있으면 3~9번을 건너뛴다.

- **정렬 기준은 키에 안 넣는다.** 같은 결과를 다르게 늘어놓을 뿐이다.
- **한국곡 포함 여부는 넣는다.** 결과 자체가 달라진다.
- `q2`의 2는 세대 번호다. 결과 내용이 달라지는 변경을 하면 올린다.

### 3. 자체 DB를 먼저 본다 (0.03초)

`songs` 테이블에서 찾는다. 8.6만 곡을 크롤링해 넣어 뒀다.

### 4. 공식을 더 찌를지 판단한다

다음 중 하나면 DB만으로는 부족하다고 본다.

| 조건 | 이유 |
| --- | --- |
| DB 결과 0건 | 정말 없는 건지 확인이 필요하다 |
| 검색어에 일본어(가나·한자)가 없다 | 한글 발음('요루시카')과 로마자('kakeru')는 **공식 검색 인덱스에만** 있다. 화면에 안 나오니 크롤링으로 못 가져온다 |

부족하면 응답에 `complete: false`를 달아 1차로 내보내고, 클라이언트가
`full=1`로 한 번 더 부른다. 2차는 공식까지 뒤져서 4초쯤 걸린다 (16번).

### 5. 공식은 브랜드마다 따로 부른다

금영은 `kysing.kr`, 태진은 `tjmedia.co.kr`. 각각 스크래핑한다.
**manana를 브랜드 없이 부르면 안 된다** — 전 기기의 합집합이 아니라
잘린 다른 데이터셋이 온다 (2번). 실패하면 manana로 폴백하고,
그것도 실패하면 만료된 캐시라도 내보낸다 (`stale: true`).

### 6. 표기를 정규화한다

`normalize_entries()`. 원본은 그대로 두고 **비교 전용 키**를 따로 만든다.
전각/반각 통일(NFKC), 괄호 안 부가정보 제거, 공백·기호 제거, 소문자화,
카타카나→히라가나 폴딩. 「レディメイド」와 「れでぃめいど」를 같은 곡으로 본다.

### 7. 세 가지를 걸러낸다

순서대로 적용한다.

| 필터 | 내용 |
| --- | --- |
| 브랜드 | 요청한 브랜드가 아니면 뺀다 |
| 한국곡 | **제목 또는 가수**가 한글 전용이면 뺀다. 설정에서 켜면 유지 (23번) |
| **가수 노이즈** | 가수 검색에서 이름만 겹친 남을 뺀다. 아래 |

### 8. 가수 노이즈 필터 (1번의 B안, 이번에 적용)

노래방 DB는 가수명을 **부분 문자열**로 찾는다. `Ado`를 검색하면
`ADOY`, `ADORA`, `Owen Ovadoz`, `X Ambassadors`가 딸려 온다.
태진 실측으로 70건 중 35건이 남이었다 (2026-08-19).

**가수명을 통째로 비교하지 않고 쪼갠 뒤 비교한다.**
구분자는 `,` `/` `&` `feat` `ft` 다 — **띄어쓰기로는 쪼개지 않는다.**
`X Ambassadors`를 공백으로 쪼개면 조각이 검색어와 맞아떨어질 수 있고,
`Sonar Pocket`처럼 이름에 공백이 든 가수가 무너진다.

```
"Ado,初音ミク"          → ["Ado", "初音ミク"]      → Ado 있음 → 남긴다
"jon-YAKITORY(Feat.Ado)" → ["jon-YAKITORY(", "Ado)"] → Ado 있음 → 남긴다
"Owen Ovadoz"           → ["Owen Ovadoz"]         → 없음 → 버린다
"ADORA(Feat.나띠)"       → ["ADORA(", "나띠)"]     → 없음 → 버린다
```

통째로 비교하면 `Ado,初音ミク`나 `아이유,박보검` 같은 합작곡이 함께
사라진다. 쪼개면 남이 걸러지면서 합작곡은 살아남는다.

**곡명 검색에는 걸지 않는다.** 곡명은 부분 일치가 오히려 필요하다.

#### 하나도 안 남으면 거르지 않는다

**한글 발음 검색 때문이다.** 금영 공식은 '미쿠'로 검색해도 곡을 찾아주는데,
돌려주는 가수명은 일본어 `初音ミク`다. 검색어와 정확히 일치할 수가 없어
그대로 거르면 **115건이 0건이 된다** (2026-08-19 실측).

| 검색어 | 금영 공식 | 정확 일치 | 실제 응답 |
| --- | --- | --- | --- |
| `Ado` (태진) | 70 | 35 | 35 (거른다) |
| `미쿠` | 115 | 0 | 115 (안 거른다) |
| `요루시카` | 20 | 0 | 20 (안 거른다) |

**한계: 한글 발음으로 검색하면 노이즈 필터가 아예 안 걸린다.**
`미쿠` 115건 안에는 `Sonar Pocket`처럼 무관한 곡이 섞여 있다.
발음 표기를 가수명과 대조할 방법이 없어서 지금은 못 거른다.
읽기(요미가나) 사전을 갖추면 풀 수 있는 문제다.

### 9. 정렬한다

기본은 **최신 발매순**, 같은 날짜면 곡번호순. 설정에서 곡번호순으로
바꿀 수 있다. 곡번호는 문자열이라 그냥 정렬하면 9가 10보다 뒤로 가므로
숫자로 바꿔 비교한다.

### 10. 캐시에 넣는다

결과가 있으면 24시간, 없으면 10분(`EMPTY_CACHE_TTL`)이다. 빈 결과를
24시간 굳히면 업스트림 일시 장애나 오타 한 번이 하루를 간다.
별도로 7일짜리 비상 사본을 남긴다 (5번).

### 11. 잘라서 내보낸다

캐시에는 전체를 담고 응답에서만 자른다. 페이지를 넘길 때마다 업스트림을
다시 부르지 않기 위해서다. 기본 50건, 최대 200건.

### 12. 같은 곡을 한 장으로 묶는다

`match_key`(6번의 비교용 키)로 브랜드를 넘어 묶는다. 태진 1 + 금영 1이면
`total`은 2지만 카드는 1장이고, `songs`가 카드 수다.
대표 표기는 먼저 등장한 것(= 최신 발매)을 쓴다.

---

## 37. 가사 전문의 권리 — 확인 중 (2026-08-19)

30번에서 금영 검색 결과 HTML에 딸려 오는 **가사 전문**을 그대로 화면에
띄웠다. 키도 요금도 필요 없고 한글 발음까지 붙어 와서 좋은 수로 보였다.
**권리를 안 보고 넣은 것이 문제였다.**

### 판단

가사는 어문저작물이고 국내는 대부분 신탁관리 대상이다. 가사를 서비스하려면
별도 이용허락이 필요하다. 금영이 자기 사이트에 띄우는 것은 **금영이 계약을
맺었기 때문**이고, 그 권리가 우리에게 따라오지 않는다.

**앱스토어 출시 여부와 무관하다.** 기준은 배포 경로가 아니라 공중송신이다.
웹앱으로 URL만 열어 둬도 같은 문제이고, 심사가 없는 만큼 오히려 권리자가
직접 발견해 통보하는 경로가 빠르다.

| 상황 | 판단 |
| --- | --- |
| 내 기기에서 나만 사용 | 사적이용 (저작권법 제30조) |
| 웹앱 공개 | 앱스토어 출시와 동일 |

(법률 자문이 아니라 조문과 일반적인 해석 기준에 따른 판단이다.)

### 검토한 대안과 결과

한때 30번에서 걷어냈던 **Musixmatch로 되돌렸다가 다시 물렸다.**

| 방안 | 왜 안 됐나 |
| --- | --- |
| Musixmatch 무료 플랜 | **개발·테스트용, 비상업적 조건.** 공개 서비스에 쓰면 약관 위반이라 "무료 발췌"라는 선택지가 성립하지 않는다 |
| Musixmatch 유료 | 발췌 30%는 후렴 전에 끊겨 노래방에서 쓸모가 없다. 쓸모없는 기능에 매월 비용 |
| 가사 빼고 링크만 | 안전하지만 이 앱의 핵심 기능 하나가 사라진다 |

### 결정: 금영 유지 + 권리자에게 문의

**현행(금영 가사)을 유지하되, 태진·금영에 이용 가능 범위를 문의한다.**
초안은 `docs/inquiry-catalog.md`에 있고 가사 항목을 포함한다.

근거가 아니라 **선택**이다. 회신 전까지 위 위험은 그대로 남는다는 것을
알고 가는 것이며, 다음을 함께 둔다.

- **끄는 스위치** — `LYRICS_ENABLED=false`면 가사만 꺼지고 검색 링크는 남는다.
  화면은 깨지지 않고 **검색 기능도 영향받지 않는다.**
  (`KYSING_ENABLED`를 내리면 금영 공식 검색까지 꺼져 신곡이 사라진다.)
- **게시중단 절차** — 이용약관 제10조. 권리자 요청 시 지체 없이 내린다.
- **문의 기록 보관** — 보낸 날짜와 화면을 남긴다. 무단이 아니라 확인을
  구했다는 사실이 남는다.

### 회신이 오면

| 회신 | 할 일 |
| --- | --- |
| 가능 / 조건부 | 조건을 여기 기록하고 표기를 맞춘다 |
| 불가 | `LYRICS_ENABLED=false`. 가사만 내리고 링크는 남는다 |
| 무응답 | 현행 유지, 3개월 뒤 재문의 |

### 곡 카탈로그는 별개 문제

크롤링한 8.6만 곡은 가사와 층이 다르다. 곡명·번호는 **사실 정보**라 저작권
대상이 아니지만 **데이터베이스제작자의 권리**(제93조)는 별도 조항이다.
같은 문의에 함께 담았다. 지키고 있는 것:

- DB를 영구 보관본이 아니라 **갱신되는 사본**으로 운영 (TTL 구조)
- 출처 표기 (설정 화면)
- 게시중단 절차 (이용약관 제10조)
- 크롤링 간격 유지 (페이지당 2초)
- **DB 덤프를 공개하지 않는다**

---

## 38. 스크롤하다 '검색 결과가 없어요'가 뜬다 (버그 수정, 2026-08-19)

**증상:** 가수 검색에서 아래로 스크롤하면, 카드가 멀쩡히 떠 있는데
'검색 결과가 없어요'가 함께 나타났다.

### 원인 — 다음 페이지를 다른 조건으로 물었다

16번의 2단계 검색은 1차(DB만)와 2차(공식 포함)의 **캐시가 분리돼 있다**
(`:quick` 접미사). `미쿠`처럼 DB가 답할 수 없는 검색어는 1차가 0건이라
곧바로 2차를 불러 115곡을 받는다. 여기까지는 정상이다.

문제는 `loadMore()`가 **`full` 플래그를 안 실어 보냈다**는 것이다.
2차 결과(115곡)를 보고 있는데 다음 장은 1차 조건으로 요청했다.

```
1페이지(full):  songs=115 returned=50 has_more=True
2페이지(quick): songs=0   returned=0  has_more=False   ← loadMore가 보내던 요청
2페이지(full):  songs=115 returned=50 has_more=True
```

`songs=0`이 그대로 `total`을 덮었다. 카드 목록은 `mergeGroups`가 이전
결과를 유지해서 남아 있었고, 화면 조건은 `total === 0`만 보고 있었다.
**그래서 카드와 '없어요'가 동시에 떴다.**

### 고친 것

1. **`usedFull` 상태를 둔다.** 지금 보고 있는 결과가 공식까지 뒤진 것인지
   기억했다가, 다음 페이지도 같은 조건으로 요청한다.
2. **빈 상태 조건에 `groups.length === 0`을 더한다.** 카드가 떠 있으면
   '없어요'라고 하지 않는다. 다음 페이지가 일시적으로 0건이어도 마찬가지다.

두 겹으로 막은 이유 — 1번이 근본 원인이지만, 2번이 없으면 업스트림이
잠깐 흔들릴 때 같은 화면이 다시 나온다.

### 왜 테스트가 못 잡았나

`useSearch`에 테스트가 없었다. 35번에서 클라이언트 테스트를 넣을 때
`usePreview`, `groups`, `api`는 덮었지만 검색 훅은 빠져 있었다.
**세션·페이지네이션처럼 상태가 여러 번 바뀌는 곳이 가장 잘 깨진다.**
`useSearch.test.ts`를 만들어 위 두 가지를 각각 검사한다.
(첫 번째 테스트는 `full` 플래그를 빼면 실패하는 것을 확인했다.)

---

## 39. 발음 검색을 배워서 공식을 그만 부른다 (구현됨, 2026-08-19)

38번의 역조회로 `미쿠` → 태진 0건 문제는 풀렸지만 **16초가 걸렸다.**
매번 그 값을 치를 이유가 없다. 답은 이미 첫 조회 결과 안에 있다.

### 왜 자체 DB가 발음 검색을 못 했나

DB에는 가수명이 **일본어 원표기로만** 들어 있다 (`初音ミク` 220곡).
발음(`미쿠`)은 금영·태진의 **검색 인덱스에만** 있고 결과 화면에는 안 나온다.
그래서 크롤링으로는 가져올 수가 없다 — 긁을 화면이 없다.

그런데 **공식에 한 번 물어보면 답이 결과 안에 들어 있다.** `미쿠`로 찾은
행의 가수명이 `初音ミク`다. 그 연결만 적어 두면 된다.

### singer_alias 테이블

```
1회차: 미쿠 → 공식 조회(16초) → 初音ミク 발견 → 저장
2회차: 미쿠 → 별칭에서 初音ミク → songs 조회 → 0.076초, 공식 호출 0회
```

실측 (2026-08-19, Redis 캐시를 비운 상태):

| | 시간 | 공식 호출 |
| --- | --- | --- |
| 1회차 | 16.6초 | 금영 + 태진 + 역조회 |
| 2회차 | **0.076초** | **0회** |

캐시(24시간)와 다른 층이다. 캐시는 만료되면 다시 공식을 치지만,
**별칭은 남아서 계속 공식을 안 치게 만든다.**

### 공식 사이트 부담을 늘리지 않으려고 둔 제약

역조회는 남의 서버를 한 번 더 치는 일이라 조건을 좁게 잡았다.

| 조건 | 이유 |
| --- | --- |
| 2차(`full`)에서만 | 1차는 빨라야 한다. 자동으로 늘어나면 안 된다 |
| 한쪽 브랜드가 0건일 때만 | 양쪽 다 답했으면 물어볼 것이 없다 |
| 검색어에 일본어가 없을 때만 | 일본어로 0건이면 표기 문제가 아니라 정말 없는 것 |
| 이름 1개만 (`SINGER_CROSS_LOOKUP_LIMIT`) | 2개로 늘렸더니 두 번째가 피처링 아티스트라 그 사람 전곡이 딸려 오고 17초가 됐다 |
| 결과가 있을 때만 저장 | 헛다리를 배우면 다음 검색이 엉뚱해진다 |

**순효과는 호출 감소다.** 같은 발음 검색이 반복될 때 1회차만 공식을 치고
그 뒤로는 안 친다. 배우기 전에는 캐시가 만료될 때마다 다시 쳤다.

### 남는 차이: 별칭 경로는 DB 기준이다

| 경로 | 결과 |
| --- | --- |
| 공식까지 (1회차) | 179곡 |
| 별칭 + DB (2회차) | 110곡 |

DB에 없는 곡만큼 적다. `complete: true`로 내보내므로 클라이언트가 2차를
자동으로 부르지는 않지만, **'더 찾아보기' 버튼은 그대로 뜬다** (24번).
원하는 곡이 안 보이면 사용자가 눌러서 공식까지 확인할 수 있다.

이 격차는 금영 카탈로그 공백(5번)이 메워지는 만큼 줄어든다.

### 안 한 것

`alias.remember()`는 검색 흐름 한가운데서 부르므로 **예외를 밖으로 내지
않는다.** 저장에 실패하면 다음 검색이 느릴 뿐 결과는 같다.
`hits`를 세어 두었으니 나중에 어떤 발음이 실제로 쓰이는지 볼 수 있다.

---

## 40. 금영 일본곡 색인 · 번역 제목 검색 (구현됨, 2026-08-19)

### 금영에 일본곡 색인이 있었다 (5번 정정)

5번에 **"금영은 국가 필터가 없어 일본곡만 열거하는 것은 불가능하다"**고
적어 두었는데 틀렸다. 검색 페이지가 아니라 '노래방 책' 쪽에 있다.

```
GET https://kysing.kr/karaoke-book/?city=jp&s_cd=2&s_value={색인문자}&s_page={n}
```

- 색인 문자 **107종** — 히라가나 전수 + 其他 + A~Z + ETC
- 페이지당 200행, **금영 일본곡 4,793곡** 전수 (2026-08-19 실측)
- 파라미터는 `keyword`가 아니라 **`s_value`**다. `keyword`로 넣으면 조용히
  전체 목록이 나와서 색인이 도는 것처럼 보인다

**가수 이름을 몰라도 빠짐없이 가져온다는 것이 핵심이다.** 기존
`fill_gap()`은 '우리가 아는 가수'로만 검색해서, 모르는 가수의 곡은 영영
DB에 못 들어왔다.

`flask --app run crawl-ky-book` — 약 15분.

#### 정중함

전량 열거는 남의 서버에 가장 부담이 큰 작업이다.

- **동시 요청 없음.** 한 장씩 순서대로 받는다
- 요청 사이 **3초**(`BOOK_REQUEST_DELAY`). 다른 크롤(0.4초)보다 훨씬 느리다
- 하루 한 번이면 초당 0.0003요청 꼴
- 색인 하나가 실패해도 그 문자만 건너뛴다 (전체를 멈추면 받은 것도 못 쓴다)

#### 잘린 제목은 넣지 않는다

금영은 **어느 페이지에서도** 30자 남짓을 넘는 제목을 온전히 주지 않는다.
색인·검색·곡번호 상세를 모두 확인했고 전부 `..`로 자른다
(예: 44418 `366LOVEダイアリー ("KING OF PRISM -Shiny..`).

그래서 보정 요청을 보내지 않는다 — 남의 서버만 치고 얻는 것이 없다.
잘린 행은 **버린다.** 저장하면 다른 소스로 온전히 들어와 있던 행을 덮어쓰고
match_key가 어긋나 태진 번호와 한 카드로 묶이지 않는다. 버려도 그 곡을 못
찾는 것은 아니다 — 검색 시 공식을 직접 조회하는 경로가 남아 있다.

### 번역 제목으로 검색 (11번 실행)

`만찬가`로 tuki.의 `晩餐歌`를 찾을 수 있어야 한다.

**공식이 주는 한국어 제목은 답이 아니다.** `만찬가`로 검색하면 태진·금영
둘 다 결과를 주지만 **태연이 부른 다른 곡**이고, `잔혹한 천사의 테제`도
한로로의 커버다. 원곡이 아니라 한국곡이므로 23번 필터가 거르는 것이 맞다.

| 방향 | 결과 |
| --- | --- |
| 검색어를 번역해서 던진다 | ✕ `만찬가` → `晩餐の歌`. 등록 제목은 `晩餐歌` |
| **카탈로그 제목을 미리 번역해 둔다** | ⭘ `title_ko` 컬럼을 찾는다 |

`db/005_title_ko.sql` — `title_ko`(표시용) + `title_ko_norm`(검색용).
곡명 검색은 원어와 번역을 **함께** 본다. 번역만 보면 일본어 검색이 죽는다.

```
flask --app run translate-titles --limit 1000
```

- 번역이 필요한 제목(가나·한자 포함) **7,900개 · 144,410자**
  → DeepL 무료 한도(월 50만 자) 안에 들어간다
- 같은 제목은 한 번만 옮긴다(`DISTINCT title`). 로마자·숫자 제목은 건너뛴다
- DeepL은 한 요청에 50개까지 받는다. 하나씩 보내면 요청만 늘고 한도는 같다
- **`upsert`가 `title_ko`를 건드리지 않는다.** 크롤은 원어만 가져오므로
  여기서 덮으면 크롤 한 번에 번역이 통째로 날아간다 (테스트로 고정)

화면에서는 원어를 크게, 번역을 그 아래 작게 둔다. 노래방 기계에 찍혀 있는
것은 원어라 그쪽이 커야 번호를 찾을 때 헷갈리지 않는다.

**아직 안 돌렸다 — `DEEPL_API_KEY`가 필요하다.** 키가 없으면 컬럼이 비어
있을 뿐 검색은 지금과 똑같이 동작한다.

---

## 41. 접속기록 (구현됨, 2026-08-20)

「개인정보의 안전성 확보조치 기준」 제8조는 개인정보처리시스템의 접속기록을
**1년 이상 보관**하라고 정한다. 가입을 받는 이상 우리도 대상이다.

방침에 "1년 보관"이라 적어 놓고 실제로 안 남기는 것이 가장 나쁜 선택이라
(거짓 기재) 구현하는 쪽을 택했다.

### 어디에 걸었나

라우트마다 호출을 흩뿌리지 않고 `after_request` 한 곳에서 처리한다.
**빠뜨린 경로가 생기면 기록에 구멍이 나기 때문이다.**

```python
_PERSONAL_PREFIXES = ("/api/auth", "/api/favorites")
```

**개인정보를 다루는 요청만 남긴다.** 곡 검색은 대상이 아니다 — 개인정보와
무관하고, 남기면 오히려 이용자의 취향 기록이 쌓인다. 최소 수집 원칙에도
어긋난다.

응답이 나간 뒤에 남기므로 결과(status)까지 함께 적힌다. 로그인 실패도
`user_id = NULL`로 남는다 — 대입 시도를 사후에 볼 수 있어야 한다.

### 남기지 않는 것

**요청 본문은 저장하지 않는다.** 비밀번호가 섞여 들어올 자리를 아예 만들지
않는다. 기록 항목은 계정 식별자 · 수행업무 · 접속 IP · 접속일시 · 처리 결과다.

### 외래키를 걸지 않았다

`access_log.user_id`는 `users`를 참조하지 않는다. 참조하면 탈퇴 시
`ON DELETE CASCADE`로 접속기록이 함께 지워져 **보관 의무를 못 지킨다.**

### 기록 실패가 요청을 막지 않는다

`access_log.record()`는 예외를 밖으로 내지 않는다. DB가 흔들려도 이용자의
로그인이 실패하면 안 된다. 못 남긴 것은 서버 로그로만 알린다.
(`test_failure_to_log_does_not_break_request`)

### 보관 기간

**400일**(약 1년 1개월). `flask --app run purge-access-log`로 지운다.
법이 정한 하한이 1년이라 여유를 조금 두되, **필요 이상으로 오래 들고 있는
것도 최소 보관 원칙에 어긋나므로** 지우는 명령을 함께 만들었다.
개인정보 처리방침의 보관 기간 표도 같은 값으로 채웠다.

### 남은 것

- **주기적 실행이 없다.** `purge-access-log`를 아직 아무도 안 돌린다.
  크롤링 자동 실행(15번)과 같은 문제이고 함께 풀어야 한다.
- 제8조는 **월 1회 이상 점검**도 요구한다. `access_log.recent()`로 볼 수는
  있으나 화면은 없다. 운영 시작 후 필요하다.
